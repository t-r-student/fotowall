import React from 'react';

class AddPhoto extends React.Component{
    render(){
        return(
            <h1>
                this is the page where we add new POST
            </h1>
        )
    }
}

export default AddPhoto;